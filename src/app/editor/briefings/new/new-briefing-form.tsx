"use client";
import { useActionState } from "react";
import { createEditorialDraftAction, initialEditorialDraftActionState } from "./actions";
export function NewBriefingForm() { const [state, action, pending] = useActionState(createEditorialDraftAction, initialEditorialDraftActionState); return <main className="editor-shell revision-shell"><header className="revision-heading"><p className="eyebrow">New Briefing</p><h1>Start with a clear first draft.</h1><p>This creates a private Draft only. Add accepted Sources and supported Claims before asking to publish.</p></header><form action={action} className="revision-slip"><fieldset disabled={pending}><label>Topic slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" placeholder="how-singapores-government-works" /></label><label>Topic title<input name="title" required /></label><label>Topic description<textarea name="description" required rows={3} /></label><label>One-sentence explanation<textarea name="oneSentenceExplanation" required /></label><label>30-second overview<textarea name="thirtySecondOverview" required /></label><label>Five-minute explanation<textarea name="fiveMinuteExplanation" required rows={8} /></label><label>Why people care<textarea name="whyPeopleCare" required /></label><label>Singapore context<textarea name="singaporeSeaAngle" required /></label><label>Key terms <small>One per line: Term :: definition</small><textarea name="keyTerms" rows={5} /></label><label>People and institutions <small>One per line</small><textarea name="entities" rows={4} /></label><label>Questions people discuss <small>One per line</small><textarea name="debates" rows={4} /></label><label>Questions to ask <small>One per line</small><textarea name="questionsToAsk" rows={4} /></label><label>Common misconceptions <small>One per line</small><textarea name="mistakesToAvoid" rows={4} /></label><p className={`revision-status revision-status-${state.kind}`} aria-live="polite">{state.message}</p><button type="submit">{pending ? "Creating Draft…" : "Create Draft"}</button></fieldset></form></main>; }
