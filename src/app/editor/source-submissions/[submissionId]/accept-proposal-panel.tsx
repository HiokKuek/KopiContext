"use client";
import { useActionState } from "react";
import type { ProposalAcceptanceActionState } from "./actions";
type Action=(state:ProposalAcceptanceActionState,formData:FormData)=>Promise<ProposalAcceptanceActionState>;
const initial:ProposalAcceptanceActionState={kind:"idle",message:""};
export function AcceptProposalPanel({suggestedTopic,action}:Readonly<{suggestedTopic:string;action:Action}>){const[state,formAction,pending]=useActionState<ProposalAcceptanceActionState,FormData>(action,initial);return <section className="accept-proposal"><p className="section-kicker">Explicit editorial decision</p><h2>Create a draft Briefing from this proposal</h2><p>This creates a new Draft Briefing only. It does not accept the submitted material as a Source or verify any candidate Claim.</p><form action={formAction}><label>New Topic slug<input name="topic-slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" defaultValue={slug(suggestedTopic)} /></label><label>Topic description<textarea name="topic-description" required maxLength={2000} rows={3} placeholder="Explain this Topic for the catalogue." /></label><label className="publish-confirmation"><input name="confirm-acceptance" type="checkbox" value="accept-proposal" required />I understand this creates a new draft that still needs evidence review and editorial approval.</label><button type="submit" disabled={pending}>{pending?"Creating draft…":"Create draft Briefing"}</button></form><p className={`accept-proposal-result ${state.kind}`} aria-live="polite">{state.kind==="idle"?"":state.kind==="success"?"Draft created.":state.message}</p></section>;}
function slug(value:string){return value.trim().toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"new-topic";}
