// Vitest executes server-side module contracts directly in Node. Next.js
// replaces `server-only` during application builds, so this harmless alias
// lets the same contract be exercised outside the framework compiler.
export {};
